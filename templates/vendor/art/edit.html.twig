{% extends 'base.html.twig' %}

{% block title %}Edit Art{% endblock %}

{% block body %}
    <h1>Edit Art</h1>

    {{ include('art/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-light" href="{{ path('vendor_art_index') }}">Retour</a>

    <a class="btn btn-outline-light" href="{{ path('vendor_art_edit', {'id': art.id}) }}">edit</a>

    {{ include('vendor/art/_delete_form.html.twig') }}

{% endblock %}
